# MetaFFI → Type Mapping Documentation

Each idl_entities directory should contain the file `type_mapping.md` which maps metaffi types to the runtime native data types.

An example can be found at: `sdk/idl_entities/python3/type_mapping.md`